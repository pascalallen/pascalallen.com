const INDEX = '/';
const LOGIN = '/login';
const FORBIDDEN = '/forbidden';
const TEMP = '/temp';
const CAMERA = '/camera';

export default Object.freeze({
  INDEX,
  LOGIN,
  FORBIDDEN,
  TEMP,
  CAMERA,
});
