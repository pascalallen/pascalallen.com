apiVersion: v1
kind: Service
metadata:
  name: pascalallen-postgres
  labels:
    name: postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    name: postgres
